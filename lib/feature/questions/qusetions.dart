import 'package:flutter/material.dart';

class Qusetions extends StatelessWidget {
  const Qusetions({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}